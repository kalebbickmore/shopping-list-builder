export default defineAppConfig({
  ui: {
    colors: {
      // Editorial Pantry palette, scales defined in app/assets/css/main.css.
      primary: 'terracotta',
      secondary: 'olive',
      neutral: 'stone'
    }
  }
})
